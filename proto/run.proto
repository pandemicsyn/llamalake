syntax = "proto3";
package gr;

service Run{
 rpc Submit(Command) returns (Result) {}
}

message Command{
 string blocks = 1;
 bool full_refresh = 2;
 bool force = 3;
}
message Result{
 repeated Job jobs = 2;
}

message Job{
 int64 id = 1;
 string job_id = 2;
 string state = 3;
}
